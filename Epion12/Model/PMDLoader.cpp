#include "../Epion12.h"
#include "../DX12/Buffer.h"
#include "PMDLoader.h"
namespace epion::Model
{
	void PMDLoader::HeaderLoad()
	{}
	void PMDLoader::VertexLoad()
	{
		//ifs->read((char*)position, sizeof(float) * 3);
		//ifs->read((char*)normal, sizeof(float) * 3);
		//ifs->read((char*)uv, sizeof(float) * 2);
		//ifs->read((char*)bone_index, sizeof(uint16_t) * 2);
		//ifs->read((char*)&bone_weight, sizeof(uint8_t));
		//ifs->read((char*)&edge_invisible, sizeof(uint8_t));

	}

}